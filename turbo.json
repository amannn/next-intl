{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["CI"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NEXTAUTH_SECRET"],
      "outputs": [
        "dist/**/*.ts",
        "dist/**/*.tsx",
        "dist/**/*.mjs",
        "dist/**/*.js",
        ".next/**",
        "!.next/cache/**"
      ]
    },
    "test": {
      "dependsOn": ["build"]
    },
    "size": {
      "dependsOn": ["build"]
    },
    "lint": {}
  }
}
