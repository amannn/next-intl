{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish prerelease --no-git-tag-version --yes"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
