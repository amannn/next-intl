{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish --yes --no-verify-access"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "4.0.0"
  }
}
