{
  "name": "root",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "prepare": "./scripts/prepare.sh",
    "publish": "lerna publish --yes --no-verify-access"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "turbo": "^1.9.3"
  }
}
