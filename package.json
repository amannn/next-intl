{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "lerna publish --canary --force-publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
