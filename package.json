{
  "name": "root",
  "private": true,
  "scripts": {
    "publish": "npm_config__auth=$NPM_AUTH lerna publish prerelease"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
